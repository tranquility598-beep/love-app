# Love App — Session Context

## Версия
1.7.0

## Что сделано (Фаза 1 завершена)
- Стабилизация модальных окон: ModalManager, синхронизация бэкдропов, rooms.js
- navigateToDM реализован в NavigationController, openDMConversation делегирован
- ES6 геттеры на window.current*, StateGuard, все прямые записи убраны 
  из app.js, rooms.js, voice.js, socket.js, roles.js
- Сокет-слушатели привязаны к навигации, handleReconnect починен.
- Архитектурный разрыв (Decoupling): NavigationController полностью развязан с сокетами и стал глупым стейт-паблишером. SocketLifecycle подписан на изменения стейта навигации.
- Ликвидация гонок через последовательную очередь (Mutex/Queue) на клиенте для сокет-операций при переходах.
- Сетевой контракт с бэкендом (Acknowledgements): server:join/leave и voice:join/leave поддерживают подтверждения (callbacks) с 5-секундным таймаутом на клиенте.

## Приоритетные баги и текущая задача (Решены)
- **Текущая задача**: Добавлен регулятор громкости и кнопка мьюта на плеер голосовых сообщений (ГС).
- **Баг 1 (ВЫСОКИЙ)**: Спам уведомлений от удалённых из друзей (исправлен).
- **Баг 2 (НИЗКИЙ)**: Ошибка видимого бэкдропа в диагностике (устранена).

## Следующий шаг
Этап 4 — Desktop Integration
Начинаем с tray в client/main.js:
- иконка в трее
- меню: Open Love, Check Updates, Quit
- badge со счётчиком непрочитанных сообщений
- иконка меняется при наличии непрочитанных

Этап 5 — Визуальный редизайн и кастомные SVG-иконки
- Замена плейсхолдеров и AI-иконок на единый кастомный пак SVG-иконок для премиального внешнего вида.

## Архитектурные решения
- NavigationController единственный владелец state
- window.current* только ES6 геттеры, запись через _commitState
- socketLifecycle.detachScope вызывается при каждом переходе
- ModalManager единственная точка открытия и закрытия модалок
- admin-app запланирован как отдельный веб-интерфейс для founder и staff
