node_modules/
.workbuddy/
temp/
server/uploads/
server/private-uploads/
.env
.eleven_key
tools/sound-design/samples/
tools/sound-design/out/
__pycache__/
*.pyc
build/dist/
.DS_Store
send to ai/
.mimocode/
.workbuddy/
opencode.json
notion/

# Настройки IDE — машинно-зависимые, в общий репозиторий не нужны
.idea/
*.iml
.vscode/

# Заметки AI-ассистентов: содержат внутренние детали инфраструктуры
# (адреса серверов, пути деплоя, имена переменных окружения).
# Репозиторий публичный — держим их только локально.
.devin/
.workbuddy-ai/
.claude/
# Локальные MCP-серверы разработчика (адреса на 127.0.0.1) — к проекту не относятся
.mcp.json

# Артефакты сборки и разовые архивы
*.zip
!docs/**/*.zip
dist/
release/

# Тестовые превью-мокапы лендинга — не боевые файлы, в публичный репозиторий не нужны
site-mock/
site-mock-claude/
opus5-site-mock/

# Рекламные видео и исходники монтажа: тяжёлые бинарники, репозиторий публичный
Love-videos/
