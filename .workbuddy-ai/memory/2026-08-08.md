# 2026-08-08

## Read-only аудит инфраструктуры Love (завершён)

Проведён полный аудит: репозиторий, VDS 87.199.197.158 (SSH пользователь love-deploy), публичные endpoints, установщик v2.0.6. Отчёт: `docs/INFRASTRUCTURE_AUDIT_2026-08-08.md`.

Ключевые выводы:
- КРИТИЧНО: статические TURN credentials в client/js/voice.js (строки 116–146), публичный репозиторий + история Git + публичный установщик v2.0.6. Требуется ротация + временные credentials через API.
- КРИТИЧНО: порт 5555 открыт напрямую в интернет (обход Nginx/TLS), приложение слушает 0.0.0.0.
- КРИТИЧНО: build/dist/win-unpacked/resources/server/.env содержит production-секреты локально (публичный релиз v2.0.6 чист — проверено распаковкой).
- ВЫСОКО: вложения >5 МБ и видео хранятся локально на VDS, не в Cloudinary → backup БД их не покрывает.
- ВЫСОКО: backup только pre-deploy, локально на том же VDS, без restore-скрипта и drill; mongod на VDS inactive → production-БД внешняя (Atlas, состояние snapshots не подтверждено).
- ВЫСОКО: /api/health не проверяет MongoDB readyState — мониторинг «слепой».
- Позитив: деплой-конвейер продуман (least privilege sudo, checksum, smoke-test, auto-rollback); npm audit чистый; 16/16 backend-тестов; TLS валиден.

Следующий шаг по стратегии: согласовать с Vexel исправления пунктов 1–4 (TURN, порт 5555, .env guard, health-check) перед запуском для друзей.

## Исправления критических рисков (выполнено, код)

Два локальных коммита (БЕЗ push): e54a0ca (security fixes) и 5f255c7 (runbook).
- TURN: новый endpoint GET /api/webrtc/ice-config (server/routes/webrtc.js) — временные HMAC-credentials по схеме coturn REST API; voice.js очищен от хардкода, запрашивает конфиг через apiFetch перед joinChannel. 6 новых тестов, всего 22/22 pass.
- JWT: публичный fallback 'love-app-secret-key-2024' убран из 4 файлов → server/utils/jwtSecret.js (prod требует >=32 симв, dev — эфемерный секрет).
- /api/health: проверяет mongoose readyState, 503 при деградации (smoke-проверено).
- HOST env для bind (production: 127.0.0.1).
- electron-builder: исключены .env; ops/check-build-secrets.cjs — guard в release.yml ДО публикации; локальный build/dist очищен от .env.
- В коммит e54a0ca включена вся накопленная незакоммиченная работа (205 файлов) — дерево было неделимым из-за зависимостей; production фактически работал на незакоммиченном коде.
- Push намеренно НЕ выполнен: сначала root-настройка coturn (static-auth-secret) + env на VDS по runbook ops/production/turn-firewall-hardening.md, иначе голос деградирует до STUN-only.
- Ожидает от Vexel: выполнение runbook от root на VDS (love-deploy не имеет прав на coturn/ufw), затем push и верификация по чек-листу из runbook.
